name: serviceB
description: Service B for demonstration
version: 2.1.0
kind: node
build:
  dockerfile: Dockerfile
  context: .
dependencies:
  - serviceC
environment:
  - NODE_ENV=staging
  - PORT=3000
