name: serviceC
description: Service C for demonstration
version: 1.5.2
kind: terraform
artifact: terraform_plan
artifact_path: services/serviceC/terraform-plan/plan.out
build:
  dockerfile: Dockerfile
  context: .
dependencies:
  - serviceD
environment:
  - NODE_ENV=development
  - PORT=4000
