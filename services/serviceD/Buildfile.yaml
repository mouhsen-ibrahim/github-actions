name: serviceD
description: Service D for demonstration
version: 3.0.1
kind: python
team: backend
artifact: docker_image
artifact_path: services/serviceD/docker.serviced.tar
artifact_load: docker_image
build:
  dockerfile: Dockerfile
  context: .
dependencies: []
environment:
  - NODE_ENV=production
  - PORT=5000
  - LOG_LEVEL=info
  - CACHE_TTL=3600
